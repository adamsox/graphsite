server {

    listen 80;
    listen [::]:80;
    listen 443 ssl http2;
    listen [::]:443 ssl http2;

    server_name 131.104.49.11;

    include snippets/self-signed.conf;
    location / {
        include uwsgi_params;
        uwsgi_pass unix:/home/sysadmin/myproject/myproject.sock;
    }
}
